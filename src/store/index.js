import { createStore } from 'vuex'
import skinModules from './skinModules'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    skin: skinModules
  }
})
